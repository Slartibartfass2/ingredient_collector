import 'package:flutter/material.dart';

/// Color for errors.
const errorColor = Color.fromARGB(255, 255, 82, 82);

/// Background color for errors.
const errorBackgroundColor = Color.fromARGB(255, 253, 234, 236);

/// Color for warnings.
const warningColor = Color.fromARGB(255, 255, 145, 0);

/// Background color for warnings.
const warningBackgroundColor = Color.fromARGB(255, 253, 241, 229);

/// Color for success.
const successColor = Color.fromARGB(255, 8, 185, 78);

/// Background color for success.
const successBackgroundColor = Color.fromARGB(255, 230, 248, 237);
