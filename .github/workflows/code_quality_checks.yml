name: Code Quality Checks

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.7.3'
          channel: 'stable'
          cache: true

      - name: Run setup script
        run: bash setup.ps1

      - name: Verify formatting
        run: |
          flutter format . --fix
          git diff --exit-code

  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.3.9'
          channel: 'stable'
          cache: true

      - name: Run setup script
        run: bash setup.ps1

      - name: Analyze project source
        run: flutter analyze

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.3.9'
          channel: 'stable'
          cache: true

      - name: Run setup script
        run: bash setup.ps1

      - name: Run tests
        run: flutter test --coverage --exclude-tags=explicit --reporter=expanded

      - name: Upload code coverage
        uses: codecov/codecov-action@v3
        with:
          directory: coverage
          flags: unittests
